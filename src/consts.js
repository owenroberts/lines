/**
 * enum for terminal point types in Drawing
 * @type {Object} "Enum"
 */
const Points = {
	END: 0,
	ADD: 1,
};

const LINES_VERSION = "2.7";

// layer props
// style props
// keyframe properties

export { Points, LINES_VERSION };